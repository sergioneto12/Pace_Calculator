import React from 'react';
import {FooterTag} from '../Styles/stylesheet';

const FooterComponent = () => {
    return (
        <FooterTag>
            <h1>Projeto sob supervisão e criação, Poá - SP</h1>
            <h2>Criado por InTec / 2021</h2>
        </FooterTag>
    );
};

export default FooterComponent;